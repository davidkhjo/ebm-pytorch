# `ebm` (top level)

Everything below is importable directly from `ebm` (samplers, losses, composition, AIS, `Trainer`, ...). Submodules `ebm.nets`, `ebm.datasets`, `ebm.eval`, `ebm.viz` keep their namespaces.

::: ebm
